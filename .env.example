# Copy to .env and fill in your values

NODE_ENV=development
LOG_LEVEL=info
ORCHESTRATOR_PORT=3000
DIALOGUE_AGENT_PORT=3001
WEB_AGENT_PORT=3002
KNOWLEDGE_AGENT_PORT=3003
HEALTH_CHECK_INTERVAL=30000
DATABASE_URL=postgresql://localhost:5432/jarvis

# ---- Chat LLM (backend): set ONE of these so the desktop/frontend chat works ----
# Option A: OpenAI (simplest)
OPENAI_API_KEY=your_key_here
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_MAX_TOKENS=4096
# OPENAI_TEMPERATURE=0.7

# Option B: Vertex AI (your own or Google model)
# VERTEX_AI_ENDPOINT_URL=https://your-endpoint.europe-west4-123456789.prediction.vertexai.goog
# VERTEX_AI_MODEL=your-model-id
# VERTEX_AI_MAX_TOKENS=4000
# VERTEX_AI_TEMPERATURE=0.7
# For dedicated endpoint: VERTEX_AI_PROJECT_NUMBER=123456789
# Auth: GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Optional: DIALOGUE_USE_CHAT_TEMPLATE=true to use <|im_start|> chat format (default: simple prompt)

CLAUDE_API_KEY=your_claude_key_here

# Web Agent Configuration
WEB_SEARCH_MAX_RESULTS=10
WEB_SEARCH_SAFE_MODE=moderate
BRAVE_API_KEY=your_brave_api_key_here

# Knowledge Agent Configuration
WEB_AGENT_URL=http://localhost:3002/api
# Google Custom Search Configuration (alternative to Brave)
# Get API key from: https://console.cloud.google.com/apis/credentials
# Create Custom Search Engine at: https://programmablesearchengine.google.com/
GOOGLE_SEARCH_API_KEY=your_google_api_key_here
GOOGLE_SEARCH_CX=your_custom_search_engine_id_here

# Search Engine Selection: 'brave', 'google', or 'auto' (default: auto)
# 'auto' prefers Google if configured, otherwise falls back to Brave
WEB_SEARCH_ENGINE=auto

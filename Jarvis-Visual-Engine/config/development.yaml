# Development Configuration

application:
  name: "Vision Engine"
  version: "1.0.0"
  debug: true
  log_level: "DEBUG"

processing:
  frame_rate: 15  # Lower for development
  resolution_strategy:
    face_recognition: "720p"
    scene_analysis: "480p"
    motion_detection: "480p"
  motion_threshold: 5.0
  face_confidence_threshold: 0.6
  smart_triggering: true
  smart_triggering_cooldown: 5  # seconds

vision_api:
  primary: "gpt4o"
  fallback: "claude"
  detail_level: "high"
  max_retries: 3
  timeout: 30  # seconds

cache:
  ttl: 3600  # 1 hour
  max_size: 1000
  enabled: true

privacy:
  data_retention_days: 30
  audit_retention_days: 365
  encryption_enabled: true
  privacy_mode_default: false

intelligence:
  pattern_learning_enabled: true
  context_awareness_enabled: true
  predictive_analysis_enabled: true
  anomaly_detection_enabled: true

features:
  spatial_memory: true
  appearance_tracking: true
  screen_assistance: true
  visual_guidance: true
  face_recognition: true

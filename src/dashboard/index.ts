/**
 * Public dashboard domain entrypoint for generated documentation.
 *
 * The dashboard surface packages the health API and WebSocket event stream used
 * to observe and operate the self-healing subsystem.
 */

export * from '../self-healing/dashboard';

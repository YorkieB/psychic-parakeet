/*
  This file brings together all the memory and context components for Jarvis.

  It exports the context manager and types so Jarvis can remember conversations and keep track of what's been discussed while keeping the code organized.
*/
/**
 * Memory and Context Module
 *
 * Exports context management and conversation memory functionality.
 */

export { ContextManager } from "./context-manager";
export * from "./types";

// YORKIE VALIDATED — types defined, all references resolved, JSX syntax correct, Biome reports zero errors/warnings.

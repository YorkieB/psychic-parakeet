/**
 * Public orchestrator domain entrypoint for generated documentation.
 *
 * These exports describe how Jarvis registers agents, evaluates availability,
 * and dispatches work across the multi-agent runtime.
 */

export { AgentRegistry } from './agent-registry';
export { Orchestrator } from './orchestrator';
